# Layout Perfection Plan
- Update `DashboardHome` cards to remove fixed `height: 320px` and instead use `flex: 1` or allow them to stretch naturally.
- Make the `ExploreLuter` tasks into large pill-shaped buttons with `background: #F8FAFC` and `border-radius: 9999px`.
- Enhance the `RecentFolders` to look like sleek pills.
- Increase the size of the Welcome text and mascot for better visual hierarchy.
